---
title: Theme Customization
description: Customize the look and feel of your Amaroad slides with themes, colors, and typography.
---

## Theme Configuration

Every deck has a `deck.config.ts` file that controls its visual appearance. The theme is defined under the `theme` key with nested `colors`, `fonts`, `spacing`, and `radius` fields:

```typescript
import { defineConfig } from "../../src/lib/deck-config";

export default defineConfig({
  title: "My Presentation",
  theme: {
    colors: {
      primary: "#e17120",
      secondary: "#ffb56a",
      accent: "#ff8c42",
      background: "#111111",
      text: "#e8e8e8",
      textMuted: "#999999",
      surface: "#1a1a1a",
      surfaceAlt: "#222222",
      border: "#333333",
    },
    fonts: {
      heading: "Inter, system-ui, sans-serif",
      body: "Noto Sans JP, sans-serif",
      mono: "Fira Code, monospace",
      headingWeight: 700,
      bodyLineHeight: 1.6,
    },
    spacing: {
      scale: 1.0,
    },
    radius: "12px",
  },
});
```

## Color Options

### Theme Colors

<ApiTable data={[
  { flag: "colors.primary", type: "string", description: "Primary brand color (required)", required: true },
  { flag: "colors.secondary", type: "string", description: "Secondary color" },
  { flag: "colors.accent", type: "string", description: "Accent/highlight color" },
  { flag: "colors.headingGradient", type: "string", description: "CSS gradient for headings" },
  { flag: "colors.background", type: "string", description: "Slide background color" },
  { flag: "colors.text", type: "string", description: "Main text color" },
  { flag: "colors.textMuted", type: "string", description: "Muted text color" },
  { flag: "colors.textSubtle", type: "string", description: "Subtle/tertiary text color" },
  { flag: "colors.surface", type: "string", description: "Card/surface background" },
  { flag: "colors.surfaceAlt", type: "string", description: "Alternate surface color" },
  { flag: "colors.border", type: "string", description: "Border color" },
  { flag: "colors.borderLight", type: "string", description: "Light border color" },
]} />

## Typography

### Font Options

<ApiTable data={[
  { flag: "fonts.heading", type: "string", description: "Heading font family" },
  { flag: "fonts.body", type: "string", description: "Body text font family" },
  { flag: "fonts.mono", type: "string", description: "Monospace font family" },
  { flag: "fonts.headingWeight", type: "number", description: "Heading font weight" },
  { flag: "fonts.headingLetterSpacing", type: "string", description: "Heading letter spacing" },
  { flag: "fonts.bodyLineHeight", type: "number", description: "Body text line height" },
  { flag: "fonts.scale", type: "number", description: "Global font scale factor" },
]} />

## Custom Backgrounds

Set per-slide backgrounds using frontmatter:

```mdx
---
type: content
background: "linear-gradient(135deg, #1a1a2e, #16213e)"
---

# Slide with Custom Background
```

### Background Options

- **Solid color**: `background: "#1a1a2e"`
- **Gradient**: `background: "linear-gradient(135deg, #1a1a2e, #16213e)"`
- **Image**: `background: "url(/images/hero.jpg)"`

## Component Styling

Built-in components automatically adapt to your theme colors. You can also override individual component styles:

```mdx
<Chart
  type="bar"
  data={chartData}
  colors={["#e17120", "#ffb56a", "#ff8c42"]}
/>
```

<Callout type="tip" title="AI Styling">
Ask your AI agent to adjust styling: "Make slide 5 use a dark gradient background with orange accent highlights."
</Callout>
